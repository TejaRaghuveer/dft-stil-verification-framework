# Formal verification automation (maps to python/verification/formal_config.py)
formal_verification:
  enabled: true
  proof_depth: 1000
  timeout: 3600
  properties_to_prove:
    - tap_fsm
    - tdo_validity
    - scan_path
    - timing
  report_format: JSON
